# Examples

Illustrative sample output so you can see what a skill produces **before** you install anything.

- [`sample-report-card.md`](sample-report-card.md) — a full `report-card` run against a fictional repo (`acme-support-agent`).

> These are hand-written illustrations of the format, not the result of a real run. Your numbers and findings will differ — and will cite real `file:line` from your repo.
